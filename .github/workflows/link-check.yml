name: Link check

on:
  workflow_dispatch:
  schedule:
    - cron: "17 3 * * 1"
  push:
    paths:
      - "README.md"
      - "CONTRIBUTING.md"
      - "MAINTENANCE.md"

permissions:
  contents: read

jobs:
  links:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: lycheeverse/lychee-action@v2
        with:
          args: >-
            --verbose --no-progress --accept 200,202,206,429
            --exclude '^https://doi\\.org/10\\.(1145|1609|3390)/'
            --exclude '^https://vbn\\.aau\\.dk/'
            --exclude '^https://www\\.sciencedirect\\.com/'
            README.md CONTRIBUTING.md MAINTENANCE.md
          fail: true
